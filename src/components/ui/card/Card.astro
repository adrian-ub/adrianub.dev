---
import { ub } from "@/utils/ub";
import type { HTMLAttributes } from "astro/types";

export type Props = HTMLAttributes<"div">;
---

<div
  class={ub("rounded-lg bg-card text-card-foreground", Astro.props.class)}
  {...Astro.props}
>
  <slot />
</div>
