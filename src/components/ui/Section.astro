---
import type { HTMLAttributes } from "astro/types";
import { ub } from "@/utils/ub";

export type Props = HTMLAttributes<"div">;

const className = Astro.props.class;
---

<section
  class={ub("flex min-h-0 flex-col gap-y-3", className)}
  {...Astro.props}
>
  <slot />
</section>
