---
import type { ResumeSchema } from "@/lib/resume-schema";
import Section from "./Section.astro";

type Props = {
  curriculum: ResumeSchema;
};

const { curriculum } = Astro.props;
---

<Section>
  <h2 class="text-xl font-bold">Sobre mí</h2>
  <div
    class="text-pretty font-mono text-sm text-muted-foreground"
    set:html={curriculum.basics?.summary}
  />
</Section>
