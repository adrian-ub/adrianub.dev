---
interface Props {
    columns?: 1 | 2
}

const { columns = 1 } = Astro.props

const gridColumns = {
    1: 'grid-cols-1',
    2: 'grid-cols-2',
}[columns];
---

<div
  class:list={['mb-4 mt-12 grid grid-cols-1 gap-x-4 gap-y-2 pl-0', gridColumns]}
>
  <slot />
</div>
